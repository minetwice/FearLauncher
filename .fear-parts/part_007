 b'                int count = ProgressKeeper.getTaskCount();\n'
 b'                if (count == 0) {\n'
 b'                    Toast.makeText(requireContext(), "No active background downloads.", Toast.LENGTH_SHORT).show();\n'
 b'                } else {\n'
 b'                    Toast.makeText(requireContext(), count + " background download task(s) active.", Toast.LENGTH_SHORT).show();\n'
 b'                }\n',
 b'                com.kdt.mcgui.ProgressLayout plD = view.findViewById(R.id.progress_layout);\n'
 b'                if (plD != null) {\n'
 b'                    plD.setVisibility(View.VISIBLE);\n'
 b'                    plD.onClick(plD);\n'
 b'                } else {\n'
 b'                    Toast.makeText(requireContext(), "No active background downloads.", Toast.LENGTH_SHORT).show();\n'
 b'                }\n')
t = rep(t,
 b'        ProgressKeeper.addTaskCountListener(mPlayStateListener, true);\n',
 b'        ProgressKeeper.addTaskCountListener(mPlayStateListener, true);\n'
 b'        // Wire the in-flow ProgressLayout (moved from LauncherActivity into the fragment layout)\n'
