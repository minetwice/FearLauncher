<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- White Background Progress Bar with Black Border -->
    <LinearLayout
        android:id="@+id/progress_status_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/_12sdp"
        android:paddingVertical="@dimen/_8sdp"
        android:background="@drawable/progress_bar_white_bg"
        android:layout_margin="4dp">

        <ProgressBar
            android:id="@+id/progress_generic_progressbar"
            android:layout_width="@dimen/_20sdp"
            android:layout_height="@dimen/_20sdp"
            android:indeterminateTint="#000000" />

        <TextView
            android:id="@+id/progress_textview"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/_8sdp"
            android:gravity="start|center_vertical"
            android:textSize="@dimen/_11ssp"
            android:textColor="#000000"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Syncing metadata..." />

        <ImageView
            android:id="@+id/progress_flip_arrow"
            android:layout_width="@dimen/_14sdp"
            android:layout_height="@dimen/_14sdp"
            android:src="@drawable/spinner_arrow"
            app:tint="#000000" />
    </LinearLayout>

    <!-- Expanded Line-by-Line Detail Dashboard -->
    <LinearLayout
        android:id="@+id/progress_linear_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:padding="@dimen/_12sdp"
        android:background="@drawable/premium_glass_bg"
        android:layout_marginTop="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ACTIVE DOWNLOADS"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/_10ssp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

    </LinearLayout>

</merge>
