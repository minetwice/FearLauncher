<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <application
        android:name="net.kdt.pojavlaunch.PojavApplication"
        android:allowBackup="true"
        android:allowNativeHeapPointerTagging="false"
        android:appCategory="game"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:isGame="true"
        android:label="Fear Launcher"
        android:process=":launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.FearLauncher"
        android:networkSecurityConfig="@xml/netconfig"
        android:enableOnBackInvokedCallback="false"
        tools:ignore="UnusedAttribute">

        <!-- Storage Check Activity -->
        <activity
            android:name="net.kdt.pojavlaunch.MissingStorageActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

        <meta-data
            android:name="android.max_aspect"
            android:value="ratio_float" />

        <!-- MAIN ENTRY POINT (Locked to Landscape) -->        <activity
            android:name="net.kdt.pojavlaunch.TestStorageActivity"
            android:exported="true"
            android:label="Fear Launcher"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LAUNCHER HOME SCREEN (Locked to Landscape) -->
        <activity
            android:name="net.kdt.pojavlaunch.LauncherActivity"
            android:label="Fear Launcher"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="landscape"
            android:resizeableActivity="false"/>

        <!-- CONTROL IMPORT ACTIVITY -->
        <activity
            android:name="net.kdt.pojavlaunch.ImportControlActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:exported="true"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="stateVisible"
            android:screenOrientation="landscape">
            <intent-filter
                android:label="Fear Launcher"
                android:scheme="content"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- ERROR & EXIT DIALOGS -->
        <activity
            android:name="net.kdt.pojavlaunch.FatalErrorActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
            
        <activity 
            android:name="net.kdt.pojavlaunch.ShowErrorActivity"            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
            
        <activity
            android:name="net.kdt.pojavlaunch.ExitActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />

        <!-- JAVA GUI INSTALLER (Sensor Landscape for Flexibility) -->
        <activity
            android:process=":gui_installer"
            android:name="net.kdt.pojavlaunch.JavaGUILauncherActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|keyboard|navigation|uiMode"
            android:screenOrientation="sensorLandscape" />

        <!-- CUSTOM CONTROLS EDITOR (Sensor Landscape) -->
        <activity
            android:name="net.kdt.pojavlaunch.CustomControlsActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:exported="false"
            android:screenOrientation="sensorLandscape">
            <intent-filter>
                <action android:name=".CustomControlsActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- MINECRAFT GAME ACTIVITY (Sensor Landscape for Gameplay) -->
        <activity
            android:name="net.kdt.pojavlaunch.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|keyboard|navigation|uiMode"
            android:launchMode="singleTop"
            android:process=":game"
            android:screenOrientation="sensorLandscape" />

        <!-- FILE PROVIDER -->
        <provider
            android:name="net.kdt.pojavlaunch.scoped.FolderProvider"
            android:authorities="@string/storageProviderAuthorities"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!-- BACKGROUND SERVICES -->
        <service 
            android:name="net.kdt.pojavlaunch.services.ProgressService"            android:foregroundServiceType="dataSync"/>
            
        <service 
            android:name="net.kdt.pojavlaunch.services.GameService" 
            android:process=":game"
            android:foregroundServiceType="specialUse">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="This application is intended to run a 3rd-party piece of software which cannot be managed using the activity's saved instance state. This service is used for background persistence of this piece of software."/>
        </service>
    </application>

    <queries>
        <package android:name="git.fear.ffmpeg"/>
        <package android:name="git.fear.angle"/>
    </queries>
</manifest>
